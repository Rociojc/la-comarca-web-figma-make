
  # Hotel App Prototype

  This is a code bundle for Hotel App Prototype. The original project is available at https://www.figma.com/design/gTrg3hHRUfN7srQkOh91hd/Hotel-App-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  